(page "index.html"
      (:require
        [auth.screen]

        [devtools.core :as devtools]))

(with-init!
  (devtools/install!))

(html
  (head)
  (body
    (main (cell= (auth.screen/content)))))
